server.port=${PORT:10000}
spring.application.name=discovery-server

# ? Don't register itself as a client
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.instance.hostname=localhost

# ? Key fix - use RestTemplate instead of Jersey
eureka.client.eureka-connection-idle-timeout-seconds=30
eureka.server.wait-time-in-ms-when-sync-empty=0
eureka.server.enable-self-preservation=false

## ? Force RestTemplate transport
#eureka.client.rest-template-timeout.connect-timeout=5000
#eureka.client.rest-template-timeout.connect-request-timeout=8000
#eureka.client.rest-template-timeout.socket-timeout=10000
#
#logging.level.com.netflix.eureka=OFF
#logging.level.com.netflix.discovery=OFF